[Unit]
Description=Panopticon image capture

[Service]
Type=oneshot
ExecStart=${PYTHON} ${SRCDIR}/panopticon_capture.py
WorkingDirectory=${SRCDIR}

# If we take more than 50s to run, consider it failed
TimeoutStartSec=50
